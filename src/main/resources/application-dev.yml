# Development Environment Configuration
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/literature_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  sql:
    init:
      mode: never
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000

# Local File Storage
file:
  upload:
    path: ./data/uploads/